@model IEnumerable<Model.specialite>

@{
    ViewBag.Title = "Index";
}

<h2>Liste des spécialités :</h2>

<p>
    @Html.ActionLink("Ajouté", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayName("Libellé")
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.lib_spe)
        </td>
        <td>
            @*@Html.ActionLink("Éditer", "Edit", new { id=item.id_spe }) |*@
            @Html.ActionLink("Détails", "Details", new { id=item.id_spe }) |
            @Html.ActionLink("Supprimer", "Delete", new { id=item.id_spe })
        </td>
    </tr>
}

</table>
