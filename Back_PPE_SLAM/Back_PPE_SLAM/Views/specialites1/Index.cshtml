@model IEnumerable<Model.specialite>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.lib_spe)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.lib_spe)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_spe }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_spe }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_spe })
        </td>
    </tr>
}

</table>
